import React from 'react';

const Circle = (props) => {
  return (
    <div className={`${props.color} rounded-full w-12 h-12 border border-2 border-slate-800`}>
        {/*connect four icon*/}
    </div>
  );
}

export default Circle;
